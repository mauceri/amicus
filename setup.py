from setuptools import setup

setup(
   name='amicus',
   version='0.0',
   description='A conversal module',
   author='Christian Mauceri, Philippe Bonnard',
   author_email='foomail@foo.example',
   packages=['amicus'],  #same as name
   install_requires=[], #external packages as dependencies
)